<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Importação de Produtos</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
    <!-- Menu superior -->
    <div class="top-menu">
        <!-- Ícone de home -->
        <div class="home-icon">
            <a href="/dashboard">
                <i class="fas fa-home"></i>
            </a>
        </div>
        <!-- Informações do usuário (somente se estiver logado) -->
        <% if (user) { %>
        <div class="user-info">
            <span><%= user.nome %></span>
            <i class="fas fa-user-circle"></i>
        </div>
        <% } %>   
    </div>


    <!-- Mensagens de sucesso e erro -->
    <!-- Mensagens de sucesso e erro -->
    <div class="success-message"><%= success_msg %></div>
    <div class="error-message"><%= error_msg %></div>


    <div class="container-import">
        <div class="form-container">
            <form action="/importar" method="post">
                <h2>Importar Produtos</h2>
                <textarea name="dados" rows="10" placeholder="Cole os dados dos produtos aqui, de acordo com modelo: Codigo de Barras; Descrição; Quantidade" required></textarea>
                <button type="submit" class="submit-button">
                    <i class="fas fa-file-upload"></i> Enviar
                </button>
            </form>
        </div>
    </div>

    <div class="logout-container">
        <a href="/logout" class="logout-btn">Sair</a>
    </div>
    <div class="back-button-container">
        <a href="/dashboard" class="logout-btn">Voltar</a>
    </div>
</body>
</html>
